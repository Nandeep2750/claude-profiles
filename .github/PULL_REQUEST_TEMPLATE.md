## What this changes

<!-- One or two sentences. Link an issue if there is one. -->

## Checklist

- [ ] Tests pass: `python3 -m unittest discover -s tests` and `bash tests/test_shell.sh`
- [ ] Linters pass: `pipx run ruff check .` and `shellcheck --severity=warning shell/claude-profiles.bash install.sh tests/test_shell.sh`
- [ ] A test covers the change
- [ ] `docs/` updated if behaviour changed
- [ ] `CHANGELOG.md` entry added under "Unreleased"

## Anything reviewers should know

<!-- Trade-offs, things you were unsure about, follow-ups. -->
